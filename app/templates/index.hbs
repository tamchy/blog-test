{{top-menu mainPage=true}}
<div class="container" style="margin-top:5%;">
	<div class="row">
		<div class="col-sm-12 offset-sm-12 col-md-12">
			<div class="row">
				<div class="col-10 col-sm-10">
					<h4>Выбор пользователя системы</h4>
					{{select-user}}
					<p>{{#link-to "blog.create" class="btn btn-warning"}}Создать блог{{/link-to}}</p>
				</div>
			</div>
			<div class="row">
				<div class="col-10 col-sm-10">
					{{#each model as |blog|}}
						<p>{{blog-object blog=blog}}</p>
					{{/each}}
				</div>
			</div>
		</div>
	</div>
</div>
